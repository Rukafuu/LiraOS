
// FORCED PRODUCTION FOR DESKTOP LAUNCH
// Un-comment to switch back to local dev
// export const API_BASE_URL = (typeof import.meta !== 'undefined' && (import.meta as any).env?.VITE_API_BASE_URL) || 'http://localhost:4000';

export const API_BASE_URL = 'https://liraos-production.up.railway.app';

export const IS_DESKTOP = true; // Assume desktop for now
